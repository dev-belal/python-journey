"""Program to Count number of zeros in a tuple"""

# Concept: The count() method counts how many times a value appears.

a = (7, 0, 8, 0, 0, 9)
zero_count = a.count(0)
print("Number of zeros in tuple:", zero_count)
